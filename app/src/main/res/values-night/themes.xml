<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Темная тема приложения (обязательно наследуется от MaterialComponents) -->
    <style name="Theme.MonoForecast" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/dark_gray</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="colorSecondary">@color/purple_200</item>
        <item name="colorOnSecondary">@color/black</item>

        <item name="android:textColor">@color/white</item>

        <!-- Window background -->
        <item name="android:windowBackground">@color/dark_gray</item>
        <item name="android:statusBarColor">@color/black</item>

        <item name="snackbarStyle">@style/Widget.App.Snackbar</item>
    </style>

    <style name="Widget.App.Snackbar" parent="Widget.MaterialComponents.Snackbar">
        <item name="backgroundTint">@color/middle_gray</item>
        <item name="android:textAppearance">@style/TextAppearance.App.Snackbar</item>
    </style>

    <style name="TextAppearance.App.Snackbar" parent="TextAppearance.MaterialComponents.Body2">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>